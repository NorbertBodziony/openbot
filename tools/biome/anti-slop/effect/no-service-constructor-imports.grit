language js(typescript)

$import where {
  $import <: r"import.*make[A-Z].*from.*",
  register_diagnostic(
    span=$import,
    message="Do not import an Effect service constructor into runtime code. Import the owning Layer, yield the contextual service, and allow its requirements to propagate to the composition root.",
    severity="error"
  )
}
