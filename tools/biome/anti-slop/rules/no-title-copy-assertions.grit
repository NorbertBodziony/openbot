language js(typescript)

`$_.toHaveAttribute("title", $_)` as $call where {
  register_diagnostic(
    span=$call,
    message="Do not assert tooltip copy in tests. See AGENTS.md \"Tests\": assert the state the tooltip explains, such as the control being disabled.",
    severity="error"
  )
}
