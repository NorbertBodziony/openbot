language js(typescript)

`$_.toContainElement($args)` as $call where {
  register_diagnostic(
    span=$call,
    message="Do not assert DOM containment in tests. See AGENTS.md \"Tests\": query the element by its role and accessible name instead of pinning where it sits in the tree.",
    severity="error"
  )
}
