import { Typography } from "heroui-native";
import { View } from "react-native";

export function AddBotScreen() {
  // Bot creation intentionally remains a visible placeholder in the mobile prototype.
  return (
    <View className="min-h-44 items-center justify-center bg-background px-5 pb-safe-offset-5 pt-5">
      <Typography.Heading type="h4">Coming soon</Typography.Heading>
    </View>
  );
}
